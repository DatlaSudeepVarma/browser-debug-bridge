{
  "name": "@browser-debug-bridge/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Manifest V3 Chrome extension for Browser Debug Bridge.",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('No tests in this package')\""
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "@browser-debug-bridge/schema": "workspace:*"
  }
}
